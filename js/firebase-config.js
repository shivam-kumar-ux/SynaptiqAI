// js/firebase-config.js — OBSOLETE & REMOVED FOR LOCAL-FIRST ARCHITECTURE
// SYNAPTIQAI is now 100% local-first using IndexedDB and user-owned AI provider keys.
// Developer Firebase credentials have been removed.
export const db = null;
export const auth = null;
